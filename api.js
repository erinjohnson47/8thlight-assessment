const config = require('./config')

module.exports = (args) => {
    console.log('this is the books api');
    // const books = GET `https://www.googleapis.com/books/v1/volumes?q="${args}"&key=${config.GOOGLE_BOOKS_API_KEY}&maxResults=5&orderBy=relevance`
} 